apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-petclinic-reactjs-backend.fullname" . }}
  labels:
{{ include "spring-petclinic-reactjs-backend.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "spring-petclinic-reactjs-backend.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
